export const CREATE_ACCOUNT_FORM_STEPS = Object.freeze({
  seed: 1,
  password: 2,
  confirm: 3,
})

export const TRANSFER_FORM_STEPS = Object.freeze({
  send: 1,
  authorize: 2,
})

export const STAKING_FORM_STEPS = Object.freeze({
  founds: 1,
  authorize: 2,
})
