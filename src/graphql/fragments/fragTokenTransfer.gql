fragment fragTokenTransfer on TokenTransfer {
  amount
  blockNumber
  fromAddressHash
  id
  logIndex
  toAddressHash
  tokenContractAddressHash
  tokenId
  timestamp,
  tokenStatus,

  amount,
  tokenId,
  token {
    symbol,
    type
  }
}
